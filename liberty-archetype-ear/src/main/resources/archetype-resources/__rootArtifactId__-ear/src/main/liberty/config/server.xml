<?xml version="1.0" encoding="UTF-8"?>
<server description="${rootArtifactId} server">

    <!-- Enable features -->
    <featureManager>
        <feature>javaee-7.0</feature>
    </featureManager>

    <!-- To access this server from a remote client add a host attribute to the following element, e.g. host="*" -->
    <httpEndpoint id="defaultHttpEndpoint"
                  httpPort="9080"
                  httpsPort="9443" />
    
    <enterpriseApplication id="${artifactId}" location="${artifactId}.ear" name="${artifactId}"/>
</server>
